<%= form_with(model: image) do |form| %>
  <% if image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(image.errors.count, "error") %> prohibited this image from being saved:</h2>

      <ul>
        <% image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="mb-3">
    <%= form.text_field :title, class:"form-control W-30", placeholder:"Title" %>
  </div>


  <div>
     <%= form.text_field :description, class:"form-control W-30", placeholder:"Description" %>
  </div>

  <div>
    <%= form.file_field :image, class:"form-control W-30", placeholder:"image" %>
  </div>

  <div class="submit-btn">
    <%= form.submit class:"btn btn-secondary" %>
  </div>
<% end %>

